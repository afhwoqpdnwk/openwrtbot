#!/bin/bash

case "$1" in
    start)
        service bot start
        ;;
    stop)
        service bot stop
        ;;
    restart)
        service bot restart
        ;;
    *)
        echo "Usage: bot {start|stop|restart}"
        exit 1
        ;;
esac

exit 0
